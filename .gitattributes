# Normalize line endings across the Windows (server) and Mac (client) sides.
* text=auto

# Must stay LF or they break on macOS / in bash.
*.sh    text eol=lf
*.plist text eol=lf

# Windows-only launchers.
*.bat text eol=crlf
*.ps1 text eol=crlf
